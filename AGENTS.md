# AI Discussion Notes — 协作开发与发布工作流 (Agent Workflow)

本文档记录本项目当前的标准开发、CI/CD 自动化构建、云端部署以及双端协同工作流。

---

## 1. 核心架构与各端角色

- **桌面端 (Chrome MV3 扩展)**：负责网页端（ChatGPT / Kimi / DeepSeek）的无感划词剪藏、百年大报式复习排版、认知雷达与 DeepSeek 密钥管理。
- **移动端 (Capacitor Android 原生 APP)**：负责随时随地自测号外、离线复习与想法批注回流；内置应用内一键在线检查更新机制。
- **云端同步网关 (Render 托管 Node.js 服务)**：位于 `server/sync-server.js`，基于单一 Secret Token 校验实现免注册、免绑定的极简增量同步，双向同步笔记数据与 DeepSeek 模型配置。

---

## 2. 标准分支开发流程

开发遵循**特性分支迭代，主分支发布**的原则：

1. **特性分支开发与验证**：
   - 所有新功能或 Bug 修复均在对应的特性分支上独立进行；
   - 提交前必须通过单元测试（`npm test`），并完成移动端原生资产的同步（`npm run build:mobile`）；
   - 涉及发版变更时，按规范在 `package.json` 中递增版本号。
2. **合并至主分支发版**：
   - 严禁未经本地测试验证直接推送到 `main` 分支；
   - 特性验证无误后，合并到主分支（`main`）并推送，触发线上自动化构建与发布；
   - 完成后切回特性分支继续后续日常迭代。

---

## 3. 自动化 CI/CD 与发版 (GitHub Actions)

- **触发规则**：流水线严格仅在主分支（`main`）收到推送时触发，特性分支的日常推送不触发发版构建。
- **构建产物与发布**：
  - 自动编译生成 Android 最新 Debug APK；
  - 自动更新 GitHub Releases 标签 `latest-mobile`，发布最新安装包及版本元数据清单；
  - Render 云端同步网关随主分支更新自动热重载部署。
