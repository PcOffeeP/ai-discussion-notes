# AI Discussion Notes — 协作开发与发布工作流 (Agent Workflow)

本文档定义跨端协同、开发验证与 CI/CD 自动化发版规范。

---

## 1. 核心架构与各端角色

- **桌面端 (Chrome MV3 扩展)**：ChatGPT / Kimi / DeepSeek 划词剪藏、百年大报排版、认知雷达与 DeepSeek 密钥管理。
- **移动端 (Capacitor Android 原生 APP)**：离线出题自测、百年大报号外、批注回流；内置应用内一键在线检查更新。
- **云端同步网关 (Render 托管 Node.js 服务)**：位于 `server/sync-server.js`，基于单一 Secret Token 校验实现免绑定极简增量双向同步。

---

## 2. 敏捷开发与分支验证流程

遵循**特性分支高频微调，主分支阶段性发版**原则，杜绝“改动小细节就全量打包安装”的低效循环：

1. **零构建快速验证（前端优先）**：
   - 95% 的移动端 UI、文案、样式及出题逻辑均为纯前端资产，**优先在本地浏览器（`http://localhost:8080/mobile.html` + 设备仿真）即时调试**，验证无误再同步，无需每次编译 APK。
2. **特性分支持续累积**：
   - 所有日常修复与新特性在对应 `feature/*` 分支独立推进并批量累积提交；
   - 阶段性发版前必须通过单元测试（`npm test`），并执行原生资产同步（`npm run build:mobile`）。
3. **主分支集中发布**：
   - 阶段性验收确认后，在 `package.json` 中规范递增版本号；
   - 合并至 `main` 分支并推送，触发线上自动化构建与发布；
   - 发布完成后切回特性分支继续后续日常迭代。

---

## 3. 自动化 CI/CD 与发版 (GitHub Actions)

- **触发规则**：严格仅在 `main` 分支推送时触发发版流水线。
- **构建与发布产物**：
  - **固定签名机制**：统一使用工程固化的 `android/app/debug.keystore` 签名，确保跨环境跨版本签名指纹永久一致，杜绝覆盖安装失败；
  - **动态版本命名**：自动生成 `AI-Discussion-Notes-v{version}.apk`，避免浏览器重名文件的“重复下载”弹窗；
  - **语义化发版**：自动发布 GitHub Release `v{version}`（标记为 latest）并注入结构化元数据供移动端零跨域解析；
  - **云端热重载**：Render 同步网关随 `main` 推送自动热重载部署。
