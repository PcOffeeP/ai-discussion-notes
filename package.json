{
  "name": "ai-discussion-notes",
  "version": "0.4.2",
  "private": true,
  "description": "Save the best things you learn from AI. (Chrome MV3 extension)",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "serve": "python3 -m http.server 8080",
    "build:mobile": "node scripts/build-mobile-dist.js && npx cap sync android",
    "start:sync": "node server/sync-server.js"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "jsdom": "^30.0.1"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.2"
  }
}
